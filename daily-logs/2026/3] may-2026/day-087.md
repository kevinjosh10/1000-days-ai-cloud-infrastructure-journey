# Day 087 – IAM Roles: Temporary Credentials & Secure AWS Access

**Date:** 27 May 2026  
**Duration:** ~2 Hours  
**Focus:** AWS IAM Roles, AWS STS, Temporary Credentials, EC2 Instance Roles, Cross-Account Access, Trust Policies, Permission Policies, IAM Role Assumption & Secure Cloud Authentication  

---

## 🎯 Objective

Understand how AWS IAM Roles work internally and learn how AWS securely provides temporary credentials to services, users and applications without hardcoding access keys.

Learn:
- IAM Roles
- Temporary security credentials
- AWS STS (Security Token Service)
- Role assumption
- EC2 instance roles
- Instance profiles
- Temporary AccessKeyId, SecretAccessKey & Session Tokens
- Metadata service
- IAM role trust policies
- IAM role permission policies
- Cross-account IAM roles
- Secure AWS authentication
- Least-privilege role design
- IAM role-based cloud architecture
- Modern cloud credential management
- Enterprise multi-account access patterns

Practice:
- Creating IAM roles
- Attaching roles to EC2
- Viewing temporary credentials
- Accessing S3 using IAM roles
- Creating trust policies
- Creating permission policies
- Building cross-account roles
- Understanding STS AssumeRole flow
- Eliminating hardcoded AWS access keys
- Designing production-grade IAM role architectures

---

## ✅ What I Completed

### 1️⃣ Learned IAM Roles Fundamentals

Studied how AWS IAM roles provide temporary credentials for secure access to AWS services.

Learned:
- IAM roles are temporary identities
- Roles are assumed when needed
- Roles eliminate hardcoded AWS keys

Visualized:

```text
IAM Role
    ↓
Temporary Identity
```

Learned:
- IAM roles are the foundation of secure AWS authentication

---

### 2️⃣ Learned About AWS STS (Security Token Service)

Studied AWS STS and how it generates temporary credentials.

Understood:
- STS creates temporary AWS credentials
- Credentials automatically expire
- STS powers IAM role assumption

Visualized:

```text
AWS STS
    ↓
Temporary Credentials
```

Learned:
- AWS uses STS internally for secure temporary access

---

### 3️⃣ Learned Temporary Credential Components

Studied the components of temporary AWS credentials.

Observed:
- AccessKeyId
- SecretAccessKey
- SessionToken
- Expiration

Visualized:

```text
Temporary Credentials
      ↓
Auto-Expiring Access
```

Learned:
- Temporary credentials are safer than permanent access keys

---

### 4️⃣ Learned Why Hardcoding Keys is Dangerous

Studied the risks of storing permanent AWS credentials inside applications.

Understood:
- Keys can leak
- Keys may never expire
- Rotation is difficult
- Compromised keys can cause major security incidents

Visualized:

```text
Hardcoded Keys
      ↓
Security Risk
```

Learned:
- Production cloud systems should always use IAM roles instead of static credentials

---

### 5️⃣ Learned IAM Role Assumption Flow

Studied how services and users assume IAM roles.

Visualized:

```text
Service/User
      ↓
Assume Role
      ↓
STS Issues Temporary Credentials
```

Learned:
- IAM roles provide temporary AWS identities dynamically

---

### 6️⃣ Learned EC2 Instance Roles

Studied how EC2 instances securely access AWS services using IAM roles.

Understood:
- EC2 assumes IAM roles automatically
- Applications inherit temporary credentials
- No credentials need to be stored manually

Visualized:

```text
EC2 Instance
      ↓
IAM Role
      ↓
Temporary AWS Access
```

Learned:
- EC2 roles are one of the most important AWS security features

---

### 7️⃣ Learned About Instance Profiles

Studied AWS instance profiles and how they connect IAM roles to EC2.

Visualized:

```text
EC2
 ↓
Instance Profile
 ↓
IAM Role
```

Learned:
- AWS internally uses instance profiles to attach IAM roles to EC2 instances

---

### 8️⃣ Created EC2-S3-ReadOnly IAM Role

Created an IAM role for EC2 with S3 read-only permissions.

Role created:

```text
EC2-S3-ReadOnly
```

Permissions added:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "s3:GetObject",
      "Resource": "*"
    }
  ]
}
```

Visualized:

```text
EC2 Role
   ↓
Read S3 Objects
```

Learned:
- IAM roles can securely grant AWS permissions to EC2 workloads

---

### 9️⃣ Attached IAM Role to EC2 Instance

Practiced attaching IAM roles to EC2 instances.

Steps performed:
- Opened EC2 console
- Selected instance
- Modified IAM role
- Attached `EC2-S3-ReadOnly`

Visualized:

```text
EC2 Instance
      ↓
Attached IAM Role
```

Learned:
- IAM roles can be attached dynamically without rebuilding EC2 instances

---

### 🔟 Learned About EC2 Metadata Service

Studied the EC2 Instance Metadata Service (IMDS).

Observed endpoint:

```text
http://169.254.169.254
```

Understood:
- EC2 exposes metadata internally
- IAM role credentials are retrieved through metadata service

Visualized:

```text
Metadata Service
        ↓
Temporary Credentials
```

Learned:
- EC2 metadata service is critical for IAM role authentication

---

### 1️⃣1️⃣ Verified IAM Role Assumption

SSH’d into EC2 and verified role credentials.

Command used:

```bash
curl http://169.254.169.254/latest/meta-data/iam/security-credentials/
```

Observed:
- IAM role name displayed

Second command used:

```bash
curl http://169.254.169.254/latest/meta-data/iam/security-credentials/EC2-S3-ReadOnly
```

Observed:
- Temporary credentials JSON output

Visualized:

```text
EC2
 ↓
Metadata Service
 ↓
Temporary Credentials
```

Learned:
- AWS automatically injects temporary credentials into EC2 instances

---

### 1️⃣2️⃣ Learned About Session Tokens

Studied AWS session tokens in temporary credentials.

Observed:
- SessionToken field included in credentials

Understood:
- Temporary credentials require session tokens
- Session tokens validate temporary access sessions

Visualized:

```text
Temporary Session
       ↓
Session Token
```

Learned:
- Session tokens are essential for temporary IAM authentication

---

### 1️⃣3️⃣ Learned How AWS SDKs Automatically Use Roles

Studied how AWS SDKs detect IAM role credentials automatically.

Observed example:

```python
import boto3

s3 = boto3.client('s3')
```

Understood:
- SDK automatically retrieves temporary credentials from metadata service

Visualized:

```text
AWS SDK
   ↓
Auto Credential Retrieval
```

Learned:
- IAM roles simplify secure application authentication dramatically

---

### 1️⃣4️⃣ Learned Cross-Account IAM Roles

Studied how AWS accounts securely share access using IAM roles.

Visualized:

```text
Account A
    ↓
Assume Role
    ↓
Account B
```

Learned:
- Cross-account roles are heavily used in enterprise AWS environments

---

### 1️⃣5️⃣ Learned Cross-Account Trust Policies

Studied trust policies allowing another AWS account to assume a role.

Observed example:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": {
        "AWS": "arn:aws:iam::111122223333:user/DevUser"
      },
      "Action": "sts:AssumeRole"
    }
  ]
}
```

Visualized:

```text
Trust Policy
      ↓
Who Can Assume Role
```

Learned:
- Trust policies define who is allowed to use a role

---

### 1️⃣6️⃣ Learned IAM Permission Policies for Roles

Studied role permission policies defining AWS access rights.

Observed example:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": "ec2:DescribeInstances",
      "Resource": "*"
    }
  ]
}
```

Visualized:

```text
Permission Policy
        ↓
What Role Can Do
```

Learned:
- Permission policies define allowed AWS actions after role assumption

---

### 1️⃣7️⃣ Learned Trust Policy vs Permission Policy

Studied the difference between trust policies and permission policies.

Visualized:

```text
Trust Policy
      ↓
WHO can assume

Permission Policy
      ↓
WHAT role can do
```

Learned:
- IAM roles use two completely separate policy systems

---

### 1️⃣8️⃣ Learned STS AssumeRole Flow

Studied how AWS users assume IAM roles using STS.

Observed command:

```bash
aws sts assume-role \
  --role-arn arn:aws:iam::444455556666:role/CrossAccountRole \
  --role-session-name DevSession
```

Understood:
- STS returns temporary credentials
- Temporary access is granted dynamically

Visualized:

```text
AssumeRole
     ↓
Temporary Access
```

Learned:
- AssumeRole is a core AWS enterprise authentication mechanism

---

### 1️⃣9️⃣ Learned IAM Role Security Best Practices

Studied secure IAM role usage patterns.

Best practices learned:
- Always use IAM roles
- Never hardcode keys
- Use least privilege
- Prefer temporary credentials
- Rotate credentials automatically
- Use IMDSv2

Visualized:

```text
Secure Cloud
      ↓
IAM Roles Everywhere
```

Learned:
- IAM roles are central to modern zero-trust cloud security

---

### 2️⃣0️⃣ Connected IAM Roles to Real Cloud Engineering

Mapped IAM roles to real-world cloud infrastructure.

Understood:
- IAM roles secure:
  - EC2
  - Lambda
  - ECS
  - EKS
  - Terraform
  - GitHub Actions
  - CI/CD pipelines
  - Multi-account AWS Organizations

Visualized:

```text
Cloud Infrastructure
         ↓
IAM Role Authentication
```

Learned:
- Modern cloud platforms rely heavily on role-based temporary authentication systems

---

## 🧠 Key Concepts Learned

- IAM Roles  
- Temporary Credentials  
- AWS STS  
- STS AssumeRole  
- EC2 Instance Roles  
- Instance Profiles  
- Temporary AccessKeyId  
- SecretAccessKey  
- SessionToken  
- Credential Expiration  
- Metadata Service  
- IMDS  
- Cross-Account Roles  
- Trust Policies  
- Permission Policies  
- IAM Role Assumption  
- Least Privilege  
- Secure AWS Authentication  
- Role-Based Access Control  
- Enterprise AWS Access Patterns  
- IAM Security Best Practices  
- Zero-Trust Cloud Authentication  

---

## 🔄 Mindset Shift

Moved from:

> Using permanent AWS access keys for authentication  

To:

> Engineering secure cloud systems using temporary role-based authentication and AWS STS credentials  

---

## 💭 Reflection

Day 87 focused on deeply understanding AWS IAM Roles, temporary credentials, AWS STS and secure cloud authentication architectures.

Built:
- Strong IAM role understanding  
- Temporary credential architecture knowledge  
- AWS STS understanding  
- EC2 role authentication skills  
- Cross-account AWS access understanding  
- Trust policy knowledge  
- Permission policy knowledge  
- Enterprise IAM architecture awareness  
- Cloud authentication debugging skills  
- Modern zero-trust cloud security understanding  

This day marked a major transition from using static AWS credentials to understanding how modern cloud infrastructure securely authenticates services, applications and users using temporary role-based access systems powered by AWS STS.

---

**Status:** Day 87 Complete  
**Streak:** 87 / 1000 🚀