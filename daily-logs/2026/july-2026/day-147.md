# Day 147 – Week 21 Review & First Real Infrastructure

**Date:** 26 July 2026
**Duration:** ~4.5 Hours
**Focus:** End of Week 21 Synthesis, Full Production VPC Provisioning (IGW, NAT, Subnets, Routing), Remote State Integration, Strict Tagging Discipline, and Portfolio Documentation

---

## 🎯 Objective

Consolidate all Terraform fundamentals learned during Week 21 by architecting, coding, and provisioning a highly available, multi-tier AWS Virtual Private Cloud (VPC) from scratch using pure Infrastructure as Code.

Learn:

- Architecting complex inter-dependent modules (VPC -> Subnets -> IGW -> Routing)
- Isolating state tracking via S3 Backends and DynamoDB Locks in a real project
- Enforcing strict enterprise tagging conventions via `locals` and `merge()`
- Creating dynamic, tightly coupled Security Groups (Web -> App -> Bastion)
- Managing multi-file Terraform architectures (`variables.tf`, `outputs.tf`, `main.tf`)
- Public vs Private subnetworking logic encoded in HCL
- Portfolio presentation standards for Infrastructure as Code repositories

Practice:

- Implementing Architecting complex inter-dependent modules (VPC -> Subnets -> IGW -> Routing) via declarative HCL coding
- Implementing Isolating state tracking via S3 Backends and DynamoDB Locks in a real project via declarative HCL coding
- Implementing Enforcing strict enterprise tagging conventions via `locals` and `merge()` via declarative HCL coding
- Implementing Creating dynamic, tightly coupled Security Groups (Web -> App -> Bastion) via declarative HCL coding
- Implementing Managing multi-file Terraform architectures (`variables.tf`, `outputs.tf`, `main.tf`) via declarative HCL coding
- Implementing Public vs Private subnetworking logic encoded in HCL via declarative HCL coding
- Implementing Portfolio presentation standards for Infrastructure as Code repositories via declarative HCL coding
- Executing the deployment of a 15+ resource VPC topology
- Verifying NAT Gateway configuration for private subnet outbound access
- Destroying local state files to prove remote state resilience
- Publishing `terraform-aws-vpc` to GitHub with a professional `README.md`

---

## ✅ What I Completed

### 1️⃣ Engineered Project Architecture Planning

Successfully architected and executed Structuring the codebase into logical, distinct files rather than a monolithic script.

Understood:

- How deeply Project Architecture Planning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`provider.tf` | `vpc.tf` | `security.tf` | `variables.tf` | `locals.tf`
```

Learned:

- To integrate Project Architecture Planning seamlessly into my standard Infrastructure as Code workflow.

---

### 2️⃣ Engineered VPC Base Deployment

Successfully architected and executed Provisioning the foundational IPv4 CIDR block that bounds the entire cloud environment.

Understood:

- How deeply VPC Base Deployment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_vpc.main -> cidr_block = '10.0.0.0/16' -> Enable DNS Support/Hostnames
```

Learned:

- To integrate VPC Base Deployment seamlessly into my standard Infrastructure as Code workflow.

---

### 3️⃣ Engineered Public Subnet Provisioning

Successfully architected and executed Using the `count` meta-argument to iterate over Availability Zones for internet-facing tiers.

Understood:

- How deeply Public Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.public -> count = 2 -> map_public_ip_on_launch = true
```

Learned:

- To integrate Public Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 4️⃣ Engineered Private Subnet Provisioning

Successfully architected and executed Deploying isolated network segments for secure backend application hosting.

Understood:

- How deeply Private Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.private -> count = 2 -> Isolated from direct inbound internet traffic
```

Learned:

- To integrate Private Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 5️⃣ Engineered Internet Gateway Attachment

Successfully architected and executed Bridging the VPC to the public internet declaratively.

Understood:

- How deeply Internet Gateway Attachment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_internet_gateway -> attached to aws_vpc.main -> The core ingress/egress node
```

Learned:

- To integrate Internet Gateway Attachment seamlessly into my standard Infrastructure as Code workflow.

---

### 6️⃣ Engineered NAT Gateway & EIPs

Successfully architected and executed Enabling outbound-only internet access for private subnets (patching, updates).

Understood:

- How deeply NAT Gateway & EIPs impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_eip -> aws_nat_gateway -> Placed in Public Subnet -> Routes Private Subnet traffic
```

Learned:

- To integrate NAT Gateway & EIPs seamlessly into my standard Infrastructure as Code workflow.

---

### 7️⃣ Engineered Route Table Logic

Successfully architected and executed Programming the routing fabric of the VPC to direct traffic to the correct gateways.

Understood:

- How deeply Route Table Logic impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Public Route Table (0.0.0.0/0 -> IGW) || Private Route Table (0.0.0.0/0 -> NAT GW)
```

Learned:

- To integrate Route Table Logic seamlessly into my standard Infrastructure as Code workflow.

---

### 8️⃣ Engineered Route Table Associations

Successfully architected and executed Binding the specific subnets to their respective routing logic.

Understood:

- How deeply Route Table Associations impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_route_table_association -> Explicitly couples subnets to route tables
```

Learned:

- To integrate Route Table Associations seamlessly into my standard Infrastructure as Code workflow.

---

### 9️⃣ Engineered Dynamic Security Groups

Successfully architected and executed Utilizing `dynamic` blocks to provision precise, non-repetitive firewall rules.

Understood:

- How deeply Dynamic Security Groups impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Web SG (80/443 Inbound) -> App SG (8080 from Web SG) -> Bastion SG (22 from My IP)
```

Learned:

- To integrate Dynamic Security Groups seamlessly into my standard Infrastructure as Code workflow.

---

### 10️⃣ Engineered Strict Tagging via `locals`

Successfully architected and executed Enforcing enterprise resource visibility by injecting mandatory metadata into every single resource.

Understood:

- How deeply Strict Tagging via `locals` impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`merge(local.common_tags, { Name = 'Public-Subnet-1' })` -> Zero untagged resources
```

Learned:

- To integrate Strict Tagging via `locals` seamlessly into my standard Infrastructure as Code workflow.

---

### 11️⃣ Engineered Remote State Integration

Successfully architected and executed Binding the new `terraform-aws-vpc` project to the highly available S3/DynamoDB backend.

Understood:

- How deeply Remote State Integration impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
terraform init -> Downloads AWS Provider -> Locks State -> Syncs with S3
```

Learned:

- To integrate Remote State Integration seamlessly into my standard Infrastructure as Code workflow.

---

### 12️⃣ Engineered The 'Big Plan' Verification

Successfully architected and executed Running `terraform plan` on a complex architecture and reviewing the dependency graph.

Understood:

- How deeply The 'Big Plan' Verification impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Plan outputs +18 resources to add -> Dependencies implicitly calculated correctly
```

Learned:

- To integrate The 'Big Plan' Verification seamlessly into my standard Infrastructure as Code workflow.

---

### 13️⃣ Engineered The 'Big Apply' Execution

Successfully architected and executed Watching Terraform orchestrate the creation of a massive network in under 3 minutes.

Understood:

- How deeply The 'Big Apply' Execution impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
NAT Gateway provisioning takes the longest (~2 mins) -> Apply completes flawlessly
```

Learned:

- To integrate The 'Big Apply' Execution seamlessly into my standard Infrastructure as Code workflow.

---

### 14️⃣ Engineered State Resiliency Test

Successfully architected and executed Actively attempting to break the local environment to prove cloud state superiority.

Understood:

- How deeply State Resiliency Test impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
rm -rf .terraform -> rm terraform.tfstate -> terraform init -> plan -> 'No changes'
```

Learned:

- To integrate State Resiliency Test seamlessly into my standard Infrastructure as Code workflow.

---

### 15️⃣ Engineered Repository Publication

Successfully architected and executed Pushing the codebase to GitHub as the first major Phase 2 portfolio project.

Understood:

- How deeply Repository Publication impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
git commit -m 'feat: initial VPC deployment' -> git push origin main
```

Learned:

- To integrate Repository Publication seamlessly into my standard Infrastructure as Code workflow.

---

### 16️⃣ Engineered Documentation Mastery

Successfully architected and executed Writing a README.md that proves engineering capability to hiring managers.

Understood:

- How deeply Documentation Mastery impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Documented the architecture, the CIDR logic, the security group topology, and the runbook
```

Learned:

- To integrate Documentation Mastery seamlessly into my standard Infrastructure as Code workflow.

---

### 17️⃣ Engineered Project Architecture Planning

Successfully architected and executed Structuring the codebase into logical, distinct files rather than a monolithic script.

Understood:

- How deeply Project Architecture Planning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`provider.tf` | `vpc.tf` | `security.tf` | `variables.tf` | `locals.tf`
```

Learned:

- To integrate Project Architecture Planning seamlessly into my standard Infrastructure as Code workflow.

---

### 18️⃣ Engineered VPC Base Deployment

Successfully architected and executed Provisioning the foundational IPv4 CIDR block that bounds the entire cloud environment.

Understood:

- How deeply VPC Base Deployment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_vpc.main -> cidr_block = '10.0.0.0/16' -> Enable DNS Support/Hostnames
```

Learned:

- To integrate VPC Base Deployment seamlessly into my standard Infrastructure as Code workflow.

---

### 19️⃣ Engineered Public Subnet Provisioning

Successfully architected and executed Using the `count` meta-argument to iterate over Availability Zones for internet-facing tiers.

Understood:

- How deeply Public Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.public -> count = 2 -> map_public_ip_on_launch = true
```

Learned:

- To integrate Public Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 20️⃣ Engineered Private Subnet Provisioning

Successfully architected and executed Deploying isolated network segments for secure backend application hosting.

Understood:

- How deeply Private Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.private -> count = 2 -> Isolated from direct inbound internet traffic
```

Learned:

- To integrate Private Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 21️⃣ Engineered Internet Gateway Attachment

Successfully architected and executed Bridging the VPC to the public internet declaratively.

Understood:

- How deeply Internet Gateway Attachment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_internet_gateway -> attached to aws_vpc.main -> The core ingress/egress node
```

Learned:

- To integrate Internet Gateway Attachment seamlessly into my standard Infrastructure as Code workflow.

---

### 22️⃣ Engineered NAT Gateway & EIPs

Successfully architected and executed Enabling outbound-only internet access for private subnets (patching, updates).

Understood:

- How deeply NAT Gateway & EIPs impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_eip -> aws_nat_gateway -> Placed in Public Subnet -> Routes Private Subnet traffic
```

Learned:

- To integrate NAT Gateway & EIPs seamlessly into my standard Infrastructure as Code workflow.

---

### 23️⃣ Engineered Route Table Logic

Successfully architected and executed Programming the routing fabric of the VPC to direct traffic to the correct gateways.

Understood:

- How deeply Route Table Logic impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Public Route Table (0.0.0.0/0 -> IGW) || Private Route Table (0.0.0.0/0 -> NAT GW)
```

Learned:

- To integrate Route Table Logic seamlessly into my standard Infrastructure as Code workflow.

---

### 24️⃣ Engineered Route Table Associations

Successfully architected and executed Binding the specific subnets to their respective routing logic.

Understood:

- How deeply Route Table Associations impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_route_table_association -> Explicitly couples subnets to route tables
```

Learned:

- To integrate Route Table Associations seamlessly into my standard Infrastructure as Code workflow.

---

### 25️⃣ Engineered Dynamic Security Groups

Successfully architected and executed Utilizing `dynamic` blocks to provision precise, non-repetitive firewall rules.

Understood:

- How deeply Dynamic Security Groups impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Web SG (80/443 Inbound) -> App SG (8080 from Web SG) -> Bastion SG (22 from My IP)
```

Learned:

- To integrate Dynamic Security Groups seamlessly into my standard Infrastructure as Code workflow.

---

### 26️⃣ Engineered Strict Tagging via `locals`

Successfully architected and executed Enforcing enterprise resource visibility by injecting mandatory metadata into every single resource.

Understood:

- How deeply Strict Tagging via `locals` impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`merge(local.common_tags, { Name = 'Public-Subnet-1' })` -> Zero untagged resources
```

Learned:

- To integrate Strict Tagging via `locals` seamlessly into my standard Infrastructure as Code workflow.

---

### 27️⃣ Engineered Remote State Integration

Successfully architected and executed Binding the new `terraform-aws-vpc` project to the highly available S3/DynamoDB backend.

Understood:

- How deeply Remote State Integration impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
terraform init -> Downloads AWS Provider -> Locks State -> Syncs with S3
```

Learned:

- To integrate Remote State Integration seamlessly into my standard Infrastructure as Code workflow.

---

### 28️⃣ Engineered The 'Big Plan' Verification

Successfully architected and executed Running `terraform plan` on a complex architecture and reviewing the dependency graph.

Understood:

- How deeply The 'Big Plan' Verification impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Plan outputs +18 resources to add -> Dependencies implicitly calculated correctly
```

Learned:

- To integrate The 'Big Plan' Verification seamlessly into my standard Infrastructure as Code workflow.

---

### 29️⃣ Engineered The 'Big Apply' Execution

Successfully architected and executed Watching Terraform orchestrate the creation of a massive network in under 3 minutes.

Understood:

- How deeply The 'Big Apply' Execution impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
NAT Gateway provisioning takes the longest (~2 mins) -> Apply completes flawlessly
```

Learned:

- To integrate The 'Big Apply' Execution seamlessly into my standard Infrastructure as Code workflow.

---

### 30️⃣ Engineered State Resiliency Test

Successfully architected and executed Actively attempting to break the local environment to prove cloud state superiority.

Understood:

- How deeply State Resiliency Test impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
rm -rf .terraform -> rm terraform.tfstate -> terraform init -> plan -> 'No changes'
```

Learned:

- To integrate State Resiliency Test seamlessly into my standard Infrastructure as Code workflow.

---

### 31️⃣ Engineered Repository Publication

Successfully architected and executed Pushing the codebase to GitHub as the first major Phase 2 portfolio project.

Understood:

- How deeply Repository Publication impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
git commit -m 'feat: initial VPC deployment' -> git push origin main
```

Learned:

- To integrate Repository Publication seamlessly into my standard Infrastructure as Code workflow.

---

### 32️⃣ Engineered Documentation Mastery

Successfully architected and executed Writing a README.md that proves engineering capability to hiring managers.

Understood:

- How deeply Documentation Mastery impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Documented the architecture, the CIDR logic, the security group topology, and the runbook
```

Learned:

- To integrate Documentation Mastery seamlessly into my standard Infrastructure as Code workflow.

---

### 33️⃣ Engineered Project Architecture Planning

Successfully architected and executed Structuring the codebase into logical, distinct files rather than a monolithic script.

Understood:

- How deeply Project Architecture Planning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`provider.tf` | `vpc.tf` | `security.tf` | `variables.tf` | `locals.tf`
```

Learned:

- To integrate Project Architecture Planning seamlessly into my standard Infrastructure as Code workflow.

---

### 34️⃣ Engineered VPC Base Deployment

Successfully architected and executed Provisioning the foundational IPv4 CIDR block that bounds the entire cloud environment.

Understood:

- How deeply VPC Base Deployment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_vpc.main -> cidr_block = '10.0.0.0/16' -> Enable DNS Support/Hostnames
```

Learned:

- To integrate VPC Base Deployment seamlessly into my standard Infrastructure as Code workflow.

---

### 35️⃣ Engineered Public Subnet Provisioning

Successfully architected and executed Using the `count` meta-argument to iterate over Availability Zones for internet-facing tiers.

Understood:

- How deeply Public Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.public -> count = 2 -> map_public_ip_on_launch = true
```

Learned:

- To integrate Public Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 36️⃣ Engineered Private Subnet Provisioning

Successfully architected and executed Deploying isolated network segments for secure backend application hosting.

Understood:

- How deeply Private Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.private -> count = 2 -> Isolated from direct inbound internet traffic
```

Learned:

- To integrate Private Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 37️⃣ Engineered Internet Gateway Attachment

Successfully architected and executed Bridging the VPC to the public internet declaratively.

Understood:

- How deeply Internet Gateway Attachment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_internet_gateway -> attached to aws_vpc.main -> The core ingress/egress node
```

Learned:

- To integrate Internet Gateway Attachment seamlessly into my standard Infrastructure as Code workflow.

---

### 38️⃣ Engineered NAT Gateway & EIPs

Successfully architected and executed Enabling outbound-only internet access for private subnets (patching, updates).

Understood:

- How deeply NAT Gateway & EIPs impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_eip -> aws_nat_gateway -> Placed in Public Subnet -> Routes Private Subnet traffic
```

Learned:

- To integrate NAT Gateway & EIPs seamlessly into my standard Infrastructure as Code workflow.

---

### 39️⃣ Engineered Route Table Logic

Successfully architected and executed Programming the routing fabric of the VPC to direct traffic to the correct gateways.

Understood:

- How deeply Route Table Logic impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Public Route Table (0.0.0.0/0 -> IGW) || Private Route Table (0.0.0.0/0 -> NAT GW)
```

Learned:

- To integrate Route Table Logic seamlessly into my standard Infrastructure as Code workflow.

---

### 40️⃣ Engineered Route Table Associations

Successfully architected and executed Binding the specific subnets to their respective routing logic.

Understood:

- How deeply Route Table Associations impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_route_table_association -> Explicitly couples subnets to route tables
```

Learned:

- To integrate Route Table Associations seamlessly into my standard Infrastructure as Code workflow.

---

### 41️⃣ Engineered Dynamic Security Groups

Successfully architected and executed Utilizing `dynamic` blocks to provision precise, non-repetitive firewall rules.

Understood:

- How deeply Dynamic Security Groups impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Web SG (80/443 Inbound) -> App SG (8080 from Web SG) -> Bastion SG (22 from My IP)
```

Learned:

- To integrate Dynamic Security Groups seamlessly into my standard Infrastructure as Code workflow.

---

### 42️⃣ Engineered Strict Tagging via `locals`

Successfully architected and executed Enforcing enterprise resource visibility by injecting mandatory metadata into every single resource.

Understood:

- How deeply Strict Tagging via `locals` impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`merge(local.common_tags, { Name = 'Public-Subnet-1' })` -> Zero untagged resources
```

Learned:

- To integrate Strict Tagging via `locals` seamlessly into my standard Infrastructure as Code workflow.

---

### 43️⃣ Engineered Remote State Integration

Successfully architected and executed Binding the new `terraform-aws-vpc` project to the highly available S3/DynamoDB backend.

Understood:

- How deeply Remote State Integration impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
terraform init -> Downloads AWS Provider -> Locks State -> Syncs with S3
```

Learned:

- To integrate Remote State Integration seamlessly into my standard Infrastructure as Code workflow.

---

### 44️⃣ Engineered The 'Big Plan' Verification

Successfully architected and executed Running `terraform plan` on a complex architecture and reviewing the dependency graph.

Understood:

- How deeply The 'Big Plan' Verification impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Plan outputs +18 resources to add -> Dependencies implicitly calculated correctly
```

Learned:

- To integrate The 'Big Plan' Verification seamlessly into my standard Infrastructure as Code workflow.

---

### 45️⃣ Engineered The 'Big Apply' Execution

Successfully architected and executed Watching Terraform orchestrate the creation of a massive network in under 3 minutes.

Understood:

- How deeply The 'Big Apply' Execution impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
NAT Gateway provisioning takes the longest (~2 mins) -> Apply completes flawlessly
```

Learned:

- To integrate The 'Big Apply' Execution seamlessly into my standard Infrastructure as Code workflow.

---

### 46️⃣ Engineered State Resiliency Test

Successfully architected and executed Actively attempting to break the local environment to prove cloud state superiority.

Understood:

- How deeply State Resiliency Test impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
rm -rf .terraform -> rm terraform.tfstate -> terraform init -> plan -> 'No changes'
```

Learned:

- To integrate State Resiliency Test seamlessly into my standard Infrastructure as Code workflow.

---

### 47️⃣ Engineered Repository Publication

Successfully architected and executed Pushing the codebase to GitHub as the first major Phase 2 portfolio project.

Understood:

- How deeply Repository Publication impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
git commit -m 'feat: initial VPC deployment' -> git push origin main
```

Learned:

- To integrate Repository Publication seamlessly into my standard Infrastructure as Code workflow.

---

### 48️⃣ Engineered Documentation Mastery

Successfully architected and executed Writing a README.md that proves engineering capability to hiring managers.

Understood:

- How deeply Documentation Mastery impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Documented the architecture, the CIDR logic, the security group topology, and the runbook
```

Learned:

- To integrate Documentation Mastery seamlessly into my standard Infrastructure as Code workflow.

---

### 49️⃣ Engineered Project Architecture Planning

Successfully architected and executed Structuring the codebase into logical, distinct files rather than a monolithic script.

Understood:

- How deeply Project Architecture Planning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`provider.tf` | `vpc.tf` | `security.tf` | `variables.tf` | `locals.tf`
```

Learned:

- To integrate Project Architecture Planning seamlessly into my standard Infrastructure as Code workflow.

---

### 50️⃣ Engineered VPC Base Deployment

Successfully architected and executed Provisioning the foundational IPv4 CIDR block that bounds the entire cloud environment.

Understood:

- How deeply VPC Base Deployment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_vpc.main -> cidr_block = '10.0.0.0/16' -> Enable DNS Support/Hostnames
```

Learned:

- To integrate VPC Base Deployment seamlessly into my standard Infrastructure as Code workflow.

---

### 51️⃣ Engineered Public Subnet Provisioning

Successfully architected and executed Using the `count` meta-argument to iterate over Availability Zones for internet-facing tiers.

Understood:

- How deeply Public Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.public -> count = 2 -> map_public_ip_on_launch = true
```

Learned:

- To integrate Public Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 52️⃣ Engineered Private Subnet Provisioning

Successfully architected and executed Deploying isolated network segments for secure backend application hosting.

Understood:

- How deeply Private Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.private -> count = 2 -> Isolated from direct inbound internet traffic
```

Learned:

- To integrate Private Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 53️⃣ Engineered Internet Gateway Attachment

Successfully architected and executed Bridging the VPC to the public internet declaratively.

Understood:

- How deeply Internet Gateway Attachment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_internet_gateway -> attached to aws_vpc.main -> The core ingress/egress node
```

Learned:

- To integrate Internet Gateway Attachment seamlessly into my standard Infrastructure as Code workflow.

---

### 54️⃣ Engineered NAT Gateway & EIPs

Successfully architected and executed Enabling outbound-only internet access for private subnets (patching, updates).

Understood:

- How deeply NAT Gateway & EIPs impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_eip -> aws_nat_gateway -> Placed in Public Subnet -> Routes Private Subnet traffic
```

Learned:

- To integrate NAT Gateway & EIPs seamlessly into my standard Infrastructure as Code workflow.

---

### 55️⃣ Engineered Route Table Logic

Successfully architected and executed Programming the routing fabric of the VPC to direct traffic to the correct gateways.

Understood:

- How deeply Route Table Logic impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Public Route Table (0.0.0.0/0 -> IGW) || Private Route Table (0.0.0.0/0 -> NAT GW)
```

Learned:

- To integrate Route Table Logic seamlessly into my standard Infrastructure as Code workflow.

---

### 56️⃣ Engineered Route Table Associations

Successfully architected and executed Binding the specific subnets to their respective routing logic.

Understood:

- How deeply Route Table Associations impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_route_table_association -> Explicitly couples subnets to route tables
```

Learned:

- To integrate Route Table Associations seamlessly into my standard Infrastructure as Code workflow.

---

### 57️⃣ Engineered Dynamic Security Groups

Successfully architected and executed Utilizing `dynamic` blocks to provision precise, non-repetitive firewall rules.

Understood:

- How deeply Dynamic Security Groups impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Web SG (80/443 Inbound) -> App SG (8080 from Web SG) -> Bastion SG (22 from My IP)
```

Learned:

- To integrate Dynamic Security Groups seamlessly into my standard Infrastructure as Code workflow.

---

### 58️⃣ Engineered Strict Tagging via `locals`

Successfully architected and executed Enforcing enterprise resource visibility by injecting mandatory metadata into every single resource.

Understood:

- How deeply Strict Tagging via `locals` impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`merge(local.common_tags, { Name = 'Public-Subnet-1' })` -> Zero untagged resources
```

Learned:

- To integrate Strict Tagging via `locals` seamlessly into my standard Infrastructure as Code workflow.

---

### 59️⃣ Engineered Remote State Integration

Successfully architected and executed Binding the new `terraform-aws-vpc` project to the highly available S3/DynamoDB backend.

Understood:

- How deeply Remote State Integration impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
terraform init -> Downloads AWS Provider -> Locks State -> Syncs with S3
```

Learned:

- To integrate Remote State Integration seamlessly into my standard Infrastructure as Code workflow.

---

### 60️⃣ Engineered The 'Big Plan' Verification

Successfully architected and executed Running `terraform plan` on a complex architecture and reviewing the dependency graph.

Understood:

- How deeply The 'Big Plan' Verification impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Plan outputs +18 resources to add -> Dependencies implicitly calculated correctly
```

Learned:

- To integrate The 'Big Plan' Verification seamlessly into my standard Infrastructure as Code workflow.

---

### 61️⃣ Engineered The 'Big Apply' Execution

Successfully architected and executed Watching Terraform orchestrate the creation of a massive network in under 3 minutes.

Understood:

- How deeply The 'Big Apply' Execution impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
NAT Gateway provisioning takes the longest (~2 mins) -> Apply completes flawlessly
```

Learned:

- To integrate The 'Big Apply' Execution seamlessly into my standard Infrastructure as Code workflow.

---

### 62️⃣ Engineered State Resiliency Test

Successfully architected and executed Actively attempting to break the local environment to prove cloud state superiority.

Understood:

- How deeply State Resiliency Test impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
rm -rf .terraform -> rm terraform.tfstate -> terraform init -> plan -> 'No changes'
```

Learned:

- To integrate State Resiliency Test seamlessly into my standard Infrastructure as Code workflow.

---

### 63️⃣ Engineered Repository Publication

Successfully architected and executed Pushing the codebase to GitHub as the first major Phase 2 portfolio project.

Understood:

- How deeply Repository Publication impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
git commit -m 'feat: initial VPC deployment' -> git push origin main
```

Learned:

- To integrate Repository Publication seamlessly into my standard Infrastructure as Code workflow.

---

### 64️⃣ Engineered Documentation Mastery

Successfully architected and executed Writing a README.md that proves engineering capability to hiring managers.

Understood:

- How deeply Documentation Mastery impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Documented the architecture, the CIDR logic, the security group topology, and the runbook
```

Learned:

- To integrate Documentation Mastery seamlessly into my standard Infrastructure as Code workflow.

---

### 65️⃣ Engineered Project Architecture Planning

Successfully architected and executed Structuring the codebase into logical, distinct files rather than a monolithic script.

Understood:

- How deeply Project Architecture Planning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`provider.tf` | `vpc.tf` | `security.tf` | `variables.tf` | `locals.tf`
```

Learned:

- To integrate Project Architecture Planning seamlessly into my standard Infrastructure as Code workflow.

---

### 66️⃣ Engineered VPC Base Deployment

Successfully architected and executed Provisioning the foundational IPv4 CIDR block that bounds the entire cloud environment.

Understood:

- How deeply VPC Base Deployment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_vpc.main -> cidr_block = '10.0.0.0/16' -> Enable DNS Support/Hostnames
```

Learned:

- To integrate VPC Base Deployment seamlessly into my standard Infrastructure as Code workflow.

---

### 67️⃣ Engineered Public Subnet Provisioning

Successfully architected and executed Using the `count` meta-argument to iterate over Availability Zones for internet-facing tiers.

Understood:

- How deeply Public Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.public -> count = 2 -> map_public_ip_on_launch = true
```

Learned:

- To integrate Public Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 68️⃣ Engineered Private Subnet Provisioning

Successfully architected and executed Deploying isolated network segments for secure backend application hosting.

Understood:

- How deeply Private Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.private -> count = 2 -> Isolated from direct inbound internet traffic
```

Learned:

- To integrate Private Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 69️⃣ Engineered Internet Gateway Attachment

Successfully architected and executed Bridging the VPC to the public internet declaratively.

Understood:

- How deeply Internet Gateway Attachment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_internet_gateway -> attached to aws_vpc.main -> The core ingress/egress node
```

Learned:

- To integrate Internet Gateway Attachment seamlessly into my standard Infrastructure as Code workflow.

---

### 70️⃣ Engineered NAT Gateway & EIPs

Successfully architected and executed Enabling outbound-only internet access for private subnets (patching, updates).

Understood:

- How deeply NAT Gateway & EIPs impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_eip -> aws_nat_gateway -> Placed in Public Subnet -> Routes Private Subnet traffic
```

Learned:

- To integrate NAT Gateway & EIPs seamlessly into my standard Infrastructure as Code workflow.

---

### 71️⃣ Engineered Route Table Logic

Successfully architected and executed Programming the routing fabric of the VPC to direct traffic to the correct gateways.

Understood:

- How deeply Route Table Logic impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Public Route Table (0.0.0.0/0 -> IGW) || Private Route Table (0.0.0.0/0 -> NAT GW)
```

Learned:

- To integrate Route Table Logic seamlessly into my standard Infrastructure as Code workflow.

---

### 72️⃣ Engineered Route Table Associations

Successfully architected and executed Binding the specific subnets to their respective routing logic.

Understood:

- How deeply Route Table Associations impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_route_table_association -> Explicitly couples subnets to route tables
```

Learned:

- To integrate Route Table Associations seamlessly into my standard Infrastructure as Code workflow.

---

### 73️⃣ Engineered Dynamic Security Groups

Successfully architected and executed Utilizing `dynamic` blocks to provision precise, non-repetitive firewall rules.

Understood:

- How deeply Dynamic Security Groups impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Web SG (80/443 Inbound) -> App SG (8080 from Web SG) -> Bastion SG (22 from My IP)
```

Learned:

- To integrate Dynamic Security Groups seamlessly into my standard Infrastructure as Code workflow.

---

### 74️⃣ Engineered Strict Tagging via `locals`

Successfully architected and executed Enforcing enterprise resource visibility by injecting mandatory metadata into every single resource.

Understood:

- How deeply Strict Tagging via `locals` impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`merge(local.common_tags, { Name = 'Public-Subnet-1' })` -> Zero untagged resources
```

Learned:

- To integrate Strict Tagging via `locals` seamlessly into my standard Infrastructure as Code workflow.

---

### 75️⃣ Engineered Remote State Integration

Successfully architected and executed Binding the new `terraform-aws-vpc` project to the highly available S3/DynamoDB backend.

Understood:

- How deeply Remote State Integration impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
terraform init -> Downloads AWS Provider -> Locks State -> Syncs with S3
```

Learned:

- To integrate Remote State Integration seamlessly into my standard Infrastructure as Code workflow.

---

### 76️⃣ Engineered The 'Big Plan' Verification

Successfully architected and executed Running `terraform plan` on a complex architecture and reviewing the dependency graph.

Understood:

- How deeply The 'Big Plan' Verification impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Plan outputs +18 resources to add -> Dependencies implicitly calculated correctly
```

Learned:

- To integrate The 'Big Plan' Verification seamlessly into my standard Infrastructure as Code workflow.

---

### 77️⃣ Engineered The 'Big Apply' Execution

Successfully architected and executed Watching Terraform orchestrate the creation of a massive network in under 3 minutes.

Understood:

- How deeply The 'Big Apply' Execution impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
NAT Gateway provisioning takes the longest (~2 mins) -> Apply completes flawlessly
```

Learned:

- To integrate The 'Big Apply' Execution seamlessly into my standard Infrastructure as Code workflow.

---

### 78️⃣ Engineered State Resiliency Test

Successfully architected and executed Actively attempting to break the local environment to prove cloud state superiority.

Understood:

- How deeply State Resiliency Test impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
rm -rf .terraform -> rm terraform.tfstate -> terraform init -> plan -> 'No changes'
```

Learned:

- To integrate State Resiliency Test seamlessly into my standard Infrastructure as Code workflow.

---

### 79️⃣ Engineered Repository Publication

Successfully architected and executed Pushing the codebase to GitHub as the first major Phase 2 portfolio project.

Understood:

- How deeply Repository Publication impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
git commit -m 'feat: initial VPC deployment' -> git push origin main
```

Learned:

- To integrate Repository Publication seamlessly into my standard Infrastructure as Code workflow.

---

### 80️⃣ Engineered Documentation Mastery

Successfully architected and executed Writing a README.md that proves engineering capability to hiring managers.

Understood:

- How deeply Documentation Mastery impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Documented the architecture, the CIDR logic, the security group topology, and the runbook
```

Learned:

- To integrate Documentation Mastery seamlessly into my standard Infrastructure as Code workflow.

---

### 81️⃣ Engineered Project Architecture Planning

Successfully architected and executed Structuring the codebase into logical, distinct files rather than a monolithic script.

Understood:

- How deeply Project Architecture Planning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`provider.tf` | `vpc.tf` | `security.tf` | `variables.tf` | `locals.tf`
```

Learned:

- To integrate Project Architecture Planning seamlessly into my standard Infrastructure as Code workflow.

---

### 82️⃣ Engineered VPC Base Deployment

Successfully architected and executed Provisioning the foundational IPv4 CIDR block that bounds the entire cloud environment.

Understood:

- How deeply VPC Base Deployment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_vpc.main -> cidr_block = '10.0.0.0/16' -> Enable DNS Support/Hostnames
```

Learned:

- To integrate VPC Base Deployment seamlessly into my standard Infrastructure as Code workflow.

---

### 83️⃣ Engineered Public Subnet Provisioning

Successfully architected and executed Using the `count` meta-argument to iterate over Availability Zones for internet-facing tiers.

Understood:

- How deeply Public Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.public -> count = 2 -> map_public_ip_on_launch = true
```

Learned:

- To integrate Public Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 84️⃣ Engineered Private Subnet Provisioning

Successfully architected and executed Deploying isolated network segments for secure backend application hosting.

Understood:

- How deeply Private Subnet Provisioning impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_subnet.private -> count = 2 -> Isolated from direct inbound internet traffic
```

Learned:

- To integrate Private Subnet Provisioning seamlessly into my standard Infrastructure as Code workflow.

---

### 85️⃣ Engineered Internet Gateway Attachment

Successfully architected and executed Bridging the VPC to the public internet declaratively.

Understood:

- How deeply Internet Gateway Attachment impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_internet_gateway -> attached to aws_vpc.main -> The core ingress/egress node
```

Learned:

- To integrate Internet Gateway Attachment seamlessly into my standard Infrastructure as Code workflow.

---

### 86️⃣ Engineered NAT Gateway & EIPs

Successfully architected and executed Enabling outbound-only internet access for private subnets (patching, updates).

Understood:

- How deeply NAT Gateway & EIPs impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_eip -> aws_nat_gateway -> Placed in Public Subnet -> Routes Private Subnet traffic
```

Learned:

- To integrate NAT Gateway & EIPs seamlessly into my standard Infrastructure as Code workflow.

---

### 87️⃣ Engineered Route Table Logic

Successfully architected and executed Programming the routing fabric of the VPC to direct traffic to the correct gateways.

Understood:

- How deeply Route Table Logic impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Public Route Table (0.0.0.0/0 -> IGW) || Private Route Table (0.0.0.0/0 -> NAT GW)
```

Learned:

- To integrate Route Table Logic seamlessly into my standard Infrastructure as Code workflow.

---

### 88️⃣ Engineered Route Table Associations

Successfully architected and executed Binding the specific subnets to their respective routing logic.

Understood:

- How deeply Route Table Associations impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
aws_route_table_association -> Explicitly couples subnets to route tables
```

Learned:

- To integrate Route Table Associations seamlessly into my standard Infrastructure as Code workflow.

---

### 89️⃣ Engineered Dynamic Security Groups

Successfully architected and executed Utilizing `dynamic` blocks to provision precise, non-repetitive firewall rules.

Understood:

- How deeply Dynamic Security Groups impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Web SG (80/443 Inbound) -> App SG (8080 from Web SG) -> Bastion SG (22 from My IP)
```

Learned:

- To integrate Dynamic Security Groups seamlessly into my standard Infrastructure as Code workflow.

---

### 90️⃣ Engineered Strict Tagging via `locals`

Successfully architected and executed Enforcing enterprise resource visibility by injecting mandatory metadata into every single resource.

Understood:

- How deeply Strict Tagging via `locals` impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
`merge(local.common_tags, { Name = 'Public-Subnet-1' })` -> Zero untagged resources
```

Learned:

- To integrate Strict Tagging via `locals` seamlessly into my standard Infrastructure as Code workflow.

---

### 91️⃣ Engineered Remote State Integration

Successfully architected and executed Binding the new `terraform-aws-vpc` project to the highly available S3/DynamoDB backend.

Understood:

- How deeply Remote State Integration impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
terraform init -> Downloads AWS Provider -> Locks State -> Syncs with S3
```

Learned:

- To integrate Remote State Integration seamlessly into my standard Infrastructure as Code workflow.

---

### 92️⃣ Engineered The 'Big Plan' Verification

Successfully architected and executed Running `terraform plan` on a complex architecture and reviewing the dependency graph.

Understood:

- How deeply The 'Big Plan' Verification impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Plan outputs +18 resources to add -> Dependencies implicitly calculated correctly
```

Learned:

- To integrate The 'Big Plan' Verification seamlessly into my standard Infrastructure as Code workflow.

---

### 93️⃣ Engineered The 'Big Apply' Execution

Successfully architected and executed Watching Terraform orchestrate the creation of a massive network in under 3 minutes.

Understood:

- How deeply The 'Big Apply' Execution impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
NAT Gateway provisioning takes the longest (~2 mins) -> Apply completes flawlessly
```

Learned:

- To integrate The 'Big Apply' Execution seamlessly into my standard Infrastructure as Code workflow.

---

### 94️⃣ Engineered State Resiliency Test

Successfully architected and executed Actively attempting to break the local environment to prove cloud state superiority.

Understood:

- How deeply State Resiliency Test impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
rm -rf .terraform -> rm terraform.tfstate -> terraform init -> plan -> 'No changes'
```

Learned:

- To integrate State Resiliency Test seamlessly into my standard Infrastructure as Code workflow.

---

### 95️⃣ Engineered Repository Publication

Successfully architected and executed Pushing the codebase to GitHub as the first major Phase 2 portfolio project.

Understood:

- How deeply Repository Publication impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
git commit -m 'feat: initial VPC deployment' -> git push origin main
```

Learned:

- To integrate Repository Publication seamlessly into my standard Infrastructure as Code workflow.

---

### 96️⃣ Engineered Documentation Mastery

Successfully architected and executed Writing a README.md that proves engineering capability to hiring managers.

Understood:

- How deeply Documentation Mastery impacts the security and reliability of a production cloud network.
- That coding infrastructure is fundamentally different from scripting; it is systems architecture in text form.
- The absolute necessity of reviewing the dependency graph before applying changes.

Architecture / Execution Flow:

```text
Documented the architecture, the CIDR logic, the security group topology, and the runbook
```

Learned:

- To integrate Documentation Mastery seamlessly into my standard Infrastructure as Code workflow.

---

## 🧠 The Systems Engineering Transformation

### Production Standard 1

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 1
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 2

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 2
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 3

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 3
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 4

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 4
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 5

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 5
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 6

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 6
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 7

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 7
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 8

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 8
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 9

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 9
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 10

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 10
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 11

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 11
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 12

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 12
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 13

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 13
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 14

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 14
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 15

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 15
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 16

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 16
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 17

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 17
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 18

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 18
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 19

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 19
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 20

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 20
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 21

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 21
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 22

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 22
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 23

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 23
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 24

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 24
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 25

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 25
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 26

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 26
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 27

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 27
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 28

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 28
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 29

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 29
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 30

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 30
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 31

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 31
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 32

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 32
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 33

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 33
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 34

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 34
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 35

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 35
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 36

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 36
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 37

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 37
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 38

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 38
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 39

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 39
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

### Production Standard 40

Understood:

- That a VPC is not just a network; it is the absolute security perimeter of the enterprise.
- That writing 150 lines of Terraform to build this VPC saves dozens of hours of manual ClickOps errors.

Visualized:

```text
Code -> Plan -> Apply -> 18 AWS Resources Provisioned -> State Secured in S3 40
```

Learned:

- I am no longer just a student of the cloud; I am a Cloud Infrastructure Engineer. Week 21 is complete.

---

