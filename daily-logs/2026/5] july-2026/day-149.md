# 🚀 Day 149 – Building a Reusable VPC Module

**Date:** 28 July 2026
**Duration:** ~1.5 Hours
**Focus:** Advanced Module Interfaces, Dynamic NAT Gateway Logic, Multi-Environment Scaffolding (Dev vs Prod), and Architectural Standardization.

---

## 🎯 Objective

Elevate Terraform logic to true software engineering by creating an environment-agnostic Virtual Private Cloud (VPC) module that adapts network topology and cost structures based on input variables.

> [!IMPORTANT]
> **Daily Insight**
> Building automated, robust infrastructure requires relentless consistency and deep understanding of component interactions.

---

## 🛠️ Execution & Architecture

### 1️⃣ Conceptualized The Module Interface Design
Architected and implemented Defining the exact API surface that my child module will expose to developers.

```text
variables.tf -> vpc_cidr, public_subnets, private_subnets, enable_nat
```

### 2️⃣ Conceptualized Multi-Environment Hierarchy
Architected and implemented Structuring the codebase so that Dev and Prod are completely physically separated.

```text
environments/dev/main.tf vs environments/prod/main.tf -> Separation of Concerns
```

### 3️⃣ Conceptualized Concurrent List Iteration
Architected and implemented Using `count.index` to map a list of CIDR blocks perfectly to a list of Availability Zones.

```text
cidr_block = var.public_cidrs[count.index] AND az = var.azs[count.index]
```

### 4️⃣ Conceptualized Dynamic Subnet Generation
Architected and implemented Writing 12 lines of code that can build 2, 6, or 100 subnets depending purely on the input list.

```text
aws_subnet.private -> count = length(var.private_subnet_cidrs)
```

---

## 📋 Daily Review Checklist

Core concepts mastered during this session:

- [x] Designing a clean, intuitive Module Interface (`variables.tf`)
- [x] Cost-optimization via conditional toggles (`single_nat_gateway = true`)
- [x] Looping across CIDR blocks and Availability Zones concurrently
- [x] Structuring multi-environment repositories (`environments/dev` vs `environments/prod`)
- [x] Exporting critical network telemetry via `outputs.tf`
- [x] Testing modular configurations safely via isolated state directories
- [x] Comparing personal code against HashiCorp community standard modules

---
*Refactored for readability, density, and FAANG-level standards.*
